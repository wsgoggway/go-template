version: "2"
sql:
  - engine: "postgresql"
    queries: "data/queries/autors.crud.sql"
    schema: "data/schemas/autors.sql"
    gen:
      go:
        package: "models"
        out: "pkg/models"
        sql_package: "pgx/v5"
