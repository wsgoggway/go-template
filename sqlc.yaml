version: "2"
sql:
  - engine: "postgresql"
    queries: "data/queries/"
    schema: "data/schemas/"
    gen:
      go:
        package: "models"
        out: "pkg/models"
        sql_package: "pgx/v5"
